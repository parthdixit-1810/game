services:
  # Web Service
  - type: web
    name: bollywood-buzz
    env: web
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
